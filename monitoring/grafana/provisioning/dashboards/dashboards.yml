apiVersion: 1

providers:
  - name: vllm-gateway
    orgId: 1
    folder: vLLM Gateway
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
