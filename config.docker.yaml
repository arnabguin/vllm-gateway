gateway:
  listen_addr: ":8080"
vllm:
  url: "http://mock-vllm:8000"
  metrics_scrape_interval: "15s"
clickhouse:
  addr: "clickhouse:9000"
  database: "vllm_attribution"
  username: "default"
  password: "devpassword"
metrics:
  emit_interval: "15s"
